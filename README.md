# project-senku
I hate doing leetcode, so this is my way of rebelling by doing DSA in a way that's fun to me.

## Repository Structure
- `include` - Public headers, intended for reuse
- `src` = Implementations of data structures
- `tests` - Basic assert-style statements for sanity checking each implementation
